# Vercel 自定义域名配置教程

> 本教程指导你将腾讯云购买的域名绑定到 Vercel 部署的项目上，获得更专业的访问地址。

---

## 你的 DNS 现状

根据你的腾讯云 DNS 记录截图，当前配置如下：

| 主机记录 | 记录类型 | 当前记录值 | 用途 | 修改建议 |
|---------|---------|-----------|------|---------|
| `@` | CNAME | `mengqihsh.icu.pages.dnsoe5.com` | **个人网站** | ✅ **保持不变** |
| `www` | CNAME | `www.mengqihsh.icu.pages.dnsoe6.com` | 个人网站（www） | 🔄 **改为 Vercel** |
| `edgeonereclaim` | TXT | `reclaim-mf24v0j86...` | 域名验证记录 | ✅ 保持不变 |

### 你的需求

- `mengqihsh.icu`（根域名）→ **保持指向个人网站**
- `www.mengqihsh.icu`（www 子域名）→ **改为指向 Vercel 演示**

---

## 第一步：在 Vercel 添加 www 域名（2 分钟）

1. 访问 [vercel.com/dashboard](https://vercel.com/dashboard)
2. 找到 **douyin-collector** 项目，进入项目页面
3. 顶部导航点击 **Settings** → 左侧选择 **Domains**
4. 在输入框中填入：`www.mengqihsh.icu`
5. 点击 **Add**
6. 系统会显示需要配置的 DNS 记录（Type: CNAME, Name: `www`, Value: `cname.vercel-dns.com`）
7. **不要关闭此页面**，保持打开

---

## 第二步：修改腾讯云 DNS 解析（2 分钟）

你已经在腾讯云有 DNS 记录，只需修改一条即可。

### 2.1 登录腾讯云控制台

1. 访问 [console.cloud.tencent.com](https://console.cloud.tencent.com)
2. 用你的方式登录

### 2.2 找到 www 记录并修改

1. 顶部搜索框搜索 "DNS解析" → 点击 "云解析 DNS"
2. 在域名列表中找到 **mengqihsh.icu**，点击右侧的 **"解析"**
3. 在记录列表中找到 **主机记录为 `www`** 的那一行
4. 点击该行右侧的 **"修改"** 按钮

### 2.3 修改记录值

将 **记录值** 从当前的：
```
www.mengqihsh.icu.pages.dnsoe6.com
```
改为：
```
cname.vercel-dns.com
```

其他字段保持不变：
- **主机记录**：`www` ✅
- **记录类型**：`CNAME` ✅
- **线路类型**：`默认` ✅
- **TTL**：`600` ✅

点击 **确认** 保存。

### 2.4 根域名保持现状

**不要修改**主机记录为 `@` 的那条记录，它继续指向你的个人网站。

---

## 第三步：等待生效并验证（5-10 分钟）

### 3.1 Vercel 自动检测

回到之前打开的 Vercel Domains 页面，等待状态从 **"Invalid Configuration"** 变为 **"Valid Configuration"**（绿色）。

### 3.2 手动验证

打开命令提示符（CMD），执行：

```bash
nslookup www.mengqihsh.icu
```

应该返回类似：
```
www.mengqihsh.icu    canonical name = cname.vercel-dns.com.
```

### 3.3 浏览器验证

访问：https://www.mengqihsh.icu

- 显示你的抖音收藏夹页面 → ✅ 成功
- 如果显示 404 → 等 5 分钟再刷新
- 如果仍然跳转到个人网站 → 强制刷新（Ctrl+F5）或清除缓存

### 3.4 同时验证根域名

访问：https://mengqihsh.icu

- 应该仍然显示你的**个人网站** → ✅ 正确
- 如果也跳转到收藏夹 → 说明误改了 `@` 记录，需要改回

---

## 第四步：配置 Vercel 自动跳转（推荐）

可选：让 Vercel 自动将不带 www 的请求跳转到带 www。

1. 在 Vercel Dashboard → Domains 页面
2. 找到 `www.mengqihsh.icu`，点击旁边的设置图标
3. 开启 **"Redirect non-www to www"** 或类似选项
4. 这样访问 `mengqihsh.icu` 会自动跳转到 `www.mengqihsh.icu`

> ⚠️ **注意**：如果你开启这个选项，`mengqihsh.icu`（根域名）就会跳转到收藏夹，不再是你的个人网站。**建议保持关闭**，让两个地址分别指向不同内容。

---

## 配置完成后的效果

| 访问地址 | 指向目标 |
|---------|---------|
| `mengqihsh.icu` | 你的个人网站（保持现状） |
| `www.mengqihsh.icu` | Vercel 部署的抖音收藏夹 Demo |

---

## 配置检查清单

- [ ] Vercel Dashboard → Domains → 已添加 `www.mengqihsh.icu`
- [ ] 腾讯云 DNS → `www` 记录值已改为 `cname.vercel-dns.com`
- [ ] `@` 记录未修改，仍指向个人网站
- [ ] Vercel Domains 状态 → 绿色 "Valid"
- [ ] 浏览器访问 `www.mengqihsh.icu` → 显示收藏夹
- [ ] 浏览器访问 `mengqihsh.icu` → 仍显示个人网站
- [ ] README.md 中的在线链接 → 已更新为 `www.mengqihsh.icu`

---

## 常见问题

### Q: 修改 www 记录后，个人网站还能访问吗？
**答**：能。根域名 `mengqihsh.icu`（@记录）保持不变，继续指向你的个人网站。

### Q: 为什么 Vercel 状态一直不绿？
**答**：DNS 传播需要时间（通常 5-10 分钟，最长 48 小时）。可以尝试：
- 在 Vercel 页面点击 **"Refresh"** 或 **"Verify"**
- 清除浏览器缓存后重新访问
- 确认修改的是 `www` 记录的值，不是 `@` 记录

### Q: 访问 www.mengqihsh.icu 显示 "404: NOT_FOUND"？
**答**：Vercel 上还未添加该域名。请确保在 Vercel Dashboard → Domains 中添加了 `www.mengqihsh.icu`。

---

## 完成后更新项目链接

域名生效后，修改 README.md 和作业文档中的在线链接：

```markdown
**在线 Demo**：https://www.mengqihsh.icu
```

这样你的个人网站和 Demo 就共存了！

---

> 如果操作中遇到问题，请告诉我当前步骤和错误提示，我帮你排查。
