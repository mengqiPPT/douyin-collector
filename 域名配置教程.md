# Vercel 自定义域名配置教程

> 本教程指导你将腾讯云购买的域名绑定到 Vercel 部署的项目上，获得更专业的访问地址。

---

## 配置总览

```
用户访问 your-domain.com
        ↓
腾讯云 DNS 解析 → CNAME → cname.vercel-dns.com
        ↓
Vercel Edge Network → 路由到你的项目
        ↓
返回网页内容
```

---

## 第一步：在 Vercel 添加自定义域名（2 分钟）

### 1.1 打开 Vercel Dashboard

1. 访问 [vercel.com/dashboard](https://vercel.com/dashboard)
2. 使用 GitHub 账号登录（就是你推代码时用的账号）
3. 在项目列表中找到 **douyin-collector** 项目，点击进入

### 1.2 进入域名设置

1. 在项目页面顶部导航栏，点击 **Settings**（设置）
2. 左侧边栏选择 **Domains**（域名）
3. 页面中央显示 "Custom Domains" 区域

### 1.3 添加域名

1. 在输入框中输入你的域名，例如：
   - 主域名：`mengqihsh.icu`
   - 或子域名：`douyin.mengqihsh.icu`
2. 点击 **Add** 按钮
3. Vercel 会显示以下信息：
   - **Type**: CNAME
   - **Name**: `www` 或 `@` 或你的子域名前缀
   - **Value**: `cname.vercel-dns.com`

   > 请完整复制这些信息，下一步在腾讯云使用。

### 1.4 等待验证（可选先看）

Vercel 会显示域名状态为 **"Invalid Configuration"**（这是正常的，因为还没配置 DNS），同时显示需要添加的 DNS 记录。

**不要关闭这个页面**，保持打开状态，接下来去腾讯云配置。

---

## 第二步：在腾讯云配置 DNS 解析（3 分钟）

### 2.1 登录腾讯云控制台

1. 访问 [console.cloud.tencent.com](https://console.cloud.tencent.com)
2. 使用微信/QQ/邮箱登录你的账号

### 2.2 进入 DNS 解析页面

路径：

```
控制台首页
  ↓ 顶部搜索框输入 "DNS解析"
  ↓ 点击 "云解析 DNS" 进入
  ↓ 在域名列表中找到你购买的域名，点击 "解析"
```

### 2.3 添加 CNAME 记录

在 DNS 解析页面，点击 **「添加记录」** 按钮，填写以下信息：

| 字段 | 填写内容 | 说明 |
|------|----------|------|
| **主机记录** | 根据你的域名类型填写 | 见下方对照表 |
| **记录类型** | CNAME | 必须选择 CNAME |
| **线路类型** | 默认 | 保持默认即可 |
| **记录值** | `cname.vercel-dns.com` | 从 Vercel 页面复制的值 |
| **TTL** | 600 | 保持默认（10 分钟） |

**主机记录对照表**：

| 你想使用的域名 | 主机记录填写 |
|---------------|-------------|
| `mengqihsh.icu` | `@` |
| `www.yourdomain.com` | `www` |
| `douyin.yourdomain.com` | `douyin` |
| `app.yourdomain.com` | `app` |

### 2.4 保存记录

点击 **确认** 或 **保存** 按钮，DNS 记录添加完成。

**如需添加 www 跳转**（推荐同时配置）：

再添加一条记录：
- 主机记录：`www`
- 记录类型：`CNAME`
- 记录值：`cname.vercel-dns.com`

这样 `yourdomain.com` 和 `www.yourdomain.com` 都能访问。

---

## 第三步：等待验证生效（5-10 分钟）

### 3.1 返回 Vercel 查看状态

1. 回到刚才打开的 Vercel Domains 页面
2. 等待 Vercel 自动检测 DNS 配置
3. 状态会从 **"Invalid Configuration"** 变为 **"Valid Configuration"**（绿色）
4. 表示域名已成功绑定！

### 3.2 手动验证

在浏览器中访问你的域名，例如 `https://mengqihsh.icu`

- 如果显示你的项目页面 → 配置成功
- 如果显示 404 或错误 → 等待 5 分钟后刷新，或检查 DNS 配置是否正确

---

## 第四步：配置 HTTPS 证书（自动完成）

Vercel 会自动为你的自定义域名签发 Let's Encrypt SSL 证书，无需手动操作。

验证方法：
1. 浏览器访问 `https://mengqihsh.icu`
2. 地址栏左侧应显示 🔒 安全锁图标
3. 点击锁图标 → 证书 → 查看是否为 Let's Encrypt 签发

---

## 常见问题排查

### Q1: Vercel 显示 "Domain is already in use"

**原因**：该域名已在其他 Vercel 账号/项目中使用。

**解决**：
- 如果域名绑定在其他项目上，先在那个项目的 Settings → Domains 中删除
- 或确认你使用的是正确的 Vercel 账号

### Q2: DNS 配置后长时间不生效（超过 30 分钟）

**排查步骤**：

1. 在命令行中执行以下命令检查 DNS 解析：

```bash
nslookup mengqihsh.icu
# 或
nslookup -type=cname mengqihsh.icu
```

应该返回 `cname.vercel-dns.com`。

2. 如果未返回正确结果：
   - 检查腾讯云 DNS 记录是否保存成功
   - 检查主机记录是否填写正确（特别是 `@` 和 `www` 的区别）
   - 确认记录类型是 CNAME，不是 A 记录或其他

3. 如果已返回正确结果但网站仍无法访问：
   - 清除浏览器缓存（Ctrl+Shift+R 强制刷新）
   - 尝试使用无痕模式/隐私窗口访问
   - 在 Vercel 页面点击 **Refresh** 或 **Verify** 按钮

### Q3: 配置了 www 但主域名无法访问

**解决**：同时配置两条记录：

| 主机记录 | 类型 | 记录值 |
|---------|------|--------|
| `@` | CNAME | `cname.vercel-dns.com` |
| `www` | CNAME | `cname.vercel-dns.com` |

然后在 Vercel 的 Domains 页面同时添加 `yourdomain.com` 和 `www.yourdomain.com`。

Vercel 会自动将其中一个重定向到另一个（推荐将 www 重定向到主域名，或反之）。

### Q4: 腾讯云 DNS 中已有 A 记录，是否需要删除？

**回答**：如果之前配置过 A 记录指向其他服务器，需要：

1. 删除旧的 A 记录（或修改为 CNAME）
2. 确保只有 CNAME 记录指向 `cname.vercel-dns.com`

**注意**：CNAME 记录和 A 记录不能共存于同一个主机记录上（如 `@` 不能同时有 A 和 CNAME）。

---

## 配置检查清单

完成配置后逐项确认：

- [ ] Vercel Dashboard → 项目 → Settings → Domains → 已添加域名
- [ ] 腾讯云 DNS 解析 → 已添加 CNAME 记录（主机记录 + cname.vercel-dns.com）
- [ ] Vercel Domains 页面 → 显示绿色 "Valid Configuration"
- [ ] 浏览器访问域名 → 正常显示项目页面
- [ ] HTTPS 证书生效 → 地址栏显示 🔒 安全锁
- [ ] 作业文档中的在线链接 → 已更新为新域名

---

## 快速参考卡片

| 步骤 | 操作位置 | 关键动作 | 预计耗时 |
|------|----------|---------|---------|
| 1 | Vercel Dashboard | 项目 → Settings → Domains → 添加域名 | 2 分钟 |
| 2 | 腾讯云控制台 | DNS 解析 → 添加 CNAME 记录 | 3 分钟 |
| 3 | Vercel Dashboard | 等待状态变绿（Valid） | 5-10 分钟 |
| 4 | 浏览器 | 访问域名验证 | 1 分钟 |
| **总计** | | | **约 15 分钟** |

---

## 配置成功后更新 README

域名生效后，记得修改 README.md 中的在线链接：

```markdown
> **在线体验**：[https://yourdomain.com](https://yourdomain.com)
```

---

## 附：Vercel 官方文档

- [Custom Domains - Vercel Docs](https://vercel.com/docs/concepts/projects/custom-domains)
- [DNS Records - Vercel Docs](https://vercel.com/docs/concepts/projects/custom-domains#step-2-configure-dns-records)

---

> 如果在配置过程中遇到任何问题，请告诉我具体的错误提示或现象，我可以进一步帮你排查。
