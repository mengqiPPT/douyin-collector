# 抖音视频收藏夹 · AI 智能分析版

## 一、项目简介与产品定位

### 1.1 项目简介

抖音视频收藏夹是一款面向知识工作者的视频内容管理工具。用户在刷抖音时经常会遇到有价值的内容（教程、科普、观点分享），但抖音原生收藏夹缺乏检索、分类和分析能力，导致收藏即"吃灰"。

本项目解决的核心痛点：
- **收藏分散** → 统一入库，结构化存储
- **无法检索** → 全文搜索（标题/描述/标签/AI摘要）
- **内容遗忘** → AI 自动生成摘要和要点，快速回顾
- **分类混乱** → 智能自动分类 + 手动调整

### 1.2 产品定位

| 维度 | 定位说明 |
|------|----------|
| **核心场景** | 个人视频知识库管理，将抖音收藏转化为可检索的学习资料库 |
| **目标用户** | 知识工作者、学习者、内容创作者——任何在抖音上获取知识并需要回顾整理的人 |
| **产品形态** | 双版本架构：本地版（主版本，长期使用）+ 在线 Demo 版（体验展示） |
| **核心价值** | 用 AI 将"被动的视频收藏"转化为"主动的知识资产" |

**在线 Demo**：https://www.mengqihsh.icu

---

## 二、核心使用流程与 AI 介入环节说明

### 2.1 整体使用流程

```
[用户粘贴抖音分享链接]
        ↓
[系统自动解析视频元数据]
        ↓
[视频入库 + 智能分类]
        ↓
[用户点击 "AI 分析"]
        ↓
[AI 引擎生成摘要 / 要点 / 标签]
        ↓
[用户通过搜索 / 分类 / AI 摘要回顾内容]
```

### 2.2 AI 介入环节详解

#### 环节 1：智能分类（入库时自动触发）

系统根据视频标题、描述、标签中的关键词，自动匹配预设分类规则，将视频归类到以下类别之一：美食、科技、教育、娱乐、生活、财经、健身、旅游、设计、编程。用户可在详情弹窗中手动调整分类。

#### 环节 2：AI 内容分析（用户手动触发）

点击视频详情中的 **"AI 分析"** 按钮后，系统执行以下分析流程：

| 步骤 | 处理内容 | AI 能力 |
|------|----------|---------|
| ① 元数据提取 | 标题、作者、描述、标签 | 本地规则引擎解析 |
| ② 内容理解 | 识别技术工具、内容类型、目标受众 | 关键词匹配 + 规则推理 |
| ③ 摘要生成 | 生成 2-3 句话的结构化摘要 | 本地规则模板 + 动态填充 |
| ④ 要点提取 | 提取 3-5 个核心知识要点 | 基于内容类型定制要点 |
| ⑤ 标签推荐 | 生成 3-5 个 AI 内容标签 | 主题 + 类型交叉推荐 |
| ⑥ 分类优化 | 基于 AI 摘要重新建议分类 | 语义匹配 |

**在线 Demo 版本**：①~⑥ 全部基于本地规则引擎完成，无需外部 API。

#### 环节 3：AI 增强检索

搜索功能不仅匹配标题/描述/标签，还匹配 **AI 生成的摘要和要点**，实现深度内容检索。例如搜索 "Python" 时，即使视频标题中没有 "Python"，只要 AI 摘要在要点中提到了 Python 相关内容，该视频也会被召回。

---

## 三、产品核心页面截图

### 截图 1：收藏视频功能界面

![收藏视频功能](screenshots/01-收藏视频功能.png)

**功能说明**：用户通过粘贴抖音分享链接，一键将视频收藏到知识库。系统自动解析视频标题、作者、描述、标签、封面图及互动数据（点赞/评论/分享数），完成元数据抓取与入库。解析过程采用 httpx 异步请求 + BeautifulSoup4 HTML 解析，自动识别抖音短链并跳转获取真实视频 ID。

---

### 截图 2：数据概览仪表盘

![数据概览仪表盘](screenshots/02-数据概览仪表盘.png)

**功能说明**：数据仪表盘按分类聚合展示收藏的视频资源，通过 ECharts 饼图可视化分类占比。支持关键词全文搜索（SQLite FTS5 跨字段检索）与分类筛选，帮助用户快速定位感兴趣的内容。视频卡片展示封面图、标题、作者、互动数据及 AI 分析状态标识（已分析/分析中/待分析）。

---

### 截图 3：AI 实际输出界面（重点）

![AI 分析结果](screenshots/03-AI分析结果.png)

**功能说明**：点击"AI 分析"后，系统自动执行内容理解流水线：① 元数据提取（标题/描述/标签）；② 内容理解（识别技术工具、内容类型、目标受众）；③ 摘要生成（基于本地规则引擎的动态模板填充）；④ 要点提取（根据内容类型定制知识要点）；⑤ 标签推荐（主题+类型交叉推荐）。上图展示了对一条 AI 绘画教程的分析结果：摘要精准概括了视频主题，要点提取了 Stable Diffusion 关键操作步骤，标签自动识别为「AI」「绘画」「教程」类别。整个分析过程无需用户配置任何 API Key，系统通过本地规则引擎完成全部 AI 功能。

---

### 截图 4：AI 分析过程与交互

![AI 分析过程](screenshots/04-AI分析过程.png)

**功能说明**：AI 分析采用异步后台处理机制：前端点击按钮后立即返回"分析中"状态，后台启动独立任务执行元数据提取→内容理解→摘要生成流水线，前端通过 3 秒轮询获取进度更新。分析完成后自动展示结果，按钮状态从"AI 分析"变为"重新分析"，支持对同一视频进行多次洞察更新。系统兼容 Vercel Serverless 环境，无需 FFmpeg 和外部 API 密钥即可完整运行。

---

## 四、技术栈清单

### 4.1 完整技术架构

| 层级 | 技术选型 | 版本 | 作用说明 |
|------|----------|------|----------|
| **后端框架** | FastAPI | 0.115+ | 高性能异步 Web 框架，自动生成 OpenAPI 文档 |
| **ASGI 服务器** | Uvicorn | 0.32+ | 异步服务器网关接口 |
| **数据库** | SQLite | 3.39+ | 轻量级关系数据库，零配置部署 |
| **全文检索** | SQLite FTS5 | 内置 | 原生全文搜索引擎，支持中文分词 |
| **前端框架** | Vue 3 | 3.4+ | 渐进式 JavaScript 框架，组合式 API |
| **UI 组件库** | Element Plus | 2.8+ | 企业级桌面端组件库 |
| **构建工具** | Vite | 5.4+ | 下一代前端构建工具，极速 HMR |
| **HTTP 客户端** | Axios | 1.7+ | 浏览器端 HTTP 请求库 |
| **数据抓取** | httpx | 0.27+ | 异步 HTTP 客户端，替代 requests |
| **HTML 解析** | BeautifulSoup4 + lxml | 4.12+ | 抖音分享页结构解析 |
| **AI 调度** | 本地规则引擎 | 自研 | 零配置 AI 分析能力 |
| **AI 提供商** | DeepSeek / Qwen / 千帆 | API | 可选 LLM 增强 |
| **音频处理** | FFmpeg | 7.0+ | 视频下载、音频提取（可选依赖） |
| **语音识别** | Whisper (OpenAI) | 本地模型 | 本地语音识别转写（可选依赖） |
| **部署平台** | Vercel | - | Serverless 部署，自动 CI/CD |

### 4.2 项目结构

```
douyin-collector/
├── backend/
│   ├── main.py              # FastAPI 入口（路由 + 业务编排）
│   ├── database.py          # 数据层（SQLite CRUD + FTS5 索引）
│   ├── parser.py            # 抖音链接解析（短链转视频ID + 元数据提取）
│   ├── analyzer.py          # AI 分析引擎（本地规则 + API 调度）
│   ├── category.py          # 自动分类器（关键词匹配规则）
│   └── ai/                  # AI 提供商封装
│       ├── provider.py      # ProviderHub 调度中心
│       ├── deepseek.py      # DeepSeek API 封装
│       └── qwen.py          # 通义千问 API 封装
├── frontend/
│   ├── src/
│   │   ├── App.vue          # 主页面（侧边栏 + 路由页 + 弹窗）
│   │   ├── api.js           # Axios API 客户端
│   │   └── components/      # 组件目录
│   │       ├── Dashboard.vue      # 数据概览仪表盘
│   │       ├── VideoLibrary.vue   # 视频库浏览页
│   │       ├── VideoCard.vue      # 视频卡片
│   │       ├── DetailDialog.vue   # 详情弹窗（含 AI 分析结果）
│   │       ├── CollectDialog.vue  # 收藏弹窗
│   │       └── Sidebar.vue        # 侧边导航栏
├── vercel.json              # Vercel 部署配置
└── README.md                # 完整产品文档
```

---

## 五、启动方式与环境变量说明

### 5.1 快速开始（本地开发）

#### 环境要求

- Python 3.12+
- Node.js 18+
- FFmpeg（可选，仅 full 模式需要）

#### 启动后端

```bash
cd backend
pip install -r requirements.txt
python main.py
```

后端启动在 `http://localhost:8000`

#### 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端启动在 `http://localhost:5173`

### 5.2 环境变量配置

本项目通过环境变量控制 AI 能力等级。所有环境变量均为**可选**，不配置时自动降级到本地规则引擎。

```bash
# ===== AI 分析增强配置（可选）=====
# 配置后将启用 "full" 模式：语音转写 + LLM 深度摘要

# 百度语音识别（用于音频转文字）
BAIDU_APP_ID=your_app_id
BAIDU_API_KEY=your_api_key
BAIDU_SECRET_KEY=your_secret_key

# 千帆大模型（用于 LLM 摘要生成）
QIANFAN_AK=your_access_key
QIANFAN_SK=your_secret_key

# DeepSeek API（备选 LLM 提供商）
DEEPSEEK_API_KEY=your_deepseek_key

# 通义千问 API（备选 LLM 提供商）
QWEN_API_KEY=your_qwen_key
```

### 5.3 AI 模式自动判定

系统启动时自动检测环境变量，动态选择 AI 工作模式：

| 检测条件 | 工作模式 | AI 能力 |
|----------|----------|---------|
| 百度语音 + 千帆 LLM 均配置 | `full` | 语音转写 + LLM 深度摘要 + 多模态封面分析 |
| 仅配置部分 API | `basic_plus` | 本地规则 + 可用 API 增强 |
| 无 API 配置（默认） | `basic` | 本地规则引擎（零配置可用） |

### 5.4 部署说明

**Vercel 在线部署**：

本项目已配置好 Vercel 部署，只需将代码推送到 GitHub，然后在 Vercel 导入项目即可自动部署。当前在线地址：https://www.mengqihsh.icu

---

## 六、API 概览

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/videos` | 收藏视频 |
| GET | `/api/videos` | 视频列表（分页 + 搜索 + 筛选） |
| GET | `/api/videos/{id}` | 视频详情 |
| DELETE | `/api/videos/{id}` | 删除视频 |
| PATCH | `/api/videos/{id}` | 更新分类 |
| POST | `/api/videos/{id}/analyze` | 触发 AI 分析 |
| GET | `/api/videos/{id}/analyze` | 查询分析状态/结果 |
| GET | `/api/categories` | 分类列表（含计数） |
| GET | `/api/env-check` | 环境检查（AI 模式/组件状态） |
| GET | `/api/proxy-image` | 封面图代理（解决跨域） |

---

## 七、截图拍摄技巧

1. **窗口大小**：浏览器调整为 1440×900 或 1280×800
2. **缩放比例**：Ctrl+0 恢复 100% 缩放
3. **截图工具**：Win+Shift+S 或浏览器开发者工具截图
4. **数据准备**：提前收藏 3-5 个不同类型的视频，确保仪表盘有内容
5. **AI 分析**：提前为至少 2 个视频点击「AI 分析」，等待完成后再截图
6. **图片格式**：保存为 PNG 或 JPG，单张控制在 2MB 以内

---

## 八、提交检查清单

- [ ] 截图数量 ≥ 3 张（建议 4 张）
- [ ] 截图清晰度足够（文字可读）
- [ ] 至少 1 张明确展示 AI 实际输出（摘要/要点/标签）
- [ ] 每张截图下方有对应的文字说明
- [ ] 文字说明解释了功能要点或操作步骤
- [ ] 图文逻辑紧密，能完整还原核心功能与使用流程
- [ ] 包含在线链接：https://www.mengqihsh.icu
- [ ] README.md 已更新为最新版本
